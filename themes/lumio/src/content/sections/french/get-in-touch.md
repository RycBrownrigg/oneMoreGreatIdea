---
enable: true
hero:
  image: "/images/get-in-touch.jpg"
  imageAlt: "Get In Touch"
  taglineLeft: "Empowering brands with tailored communication and digital solutions"
  taglineRight: "Let's work together to create some amazing results"
  playLabel: "PLAY"
  video:
    src: "rFVpSwgCkCo" # Locally hosted video path (ex: /videos/test-video.mp4), or a YouTube/Vimeo video ID (ex: youtube id - , vimeo id - 1003013057)
    type: "" # If video is stored locally in `public/videos`, set to video file type (e.g., "video/mp4")
    provider: "youtube" # Options: "youtube", "vimeo", or "html5"
    poster: "/images/video-thumbnail.jpg" # Path to thumbnail image for the video
    autoplay: true # Set to true to autoplay; false for manual start (default: false)
    id: "get-in-touch-video"
  button:
    # Refer to the `sharedButton` schema in `src/sections.schema.ts` for all available configuration options
    # (e.g., enable, label, url, hoverEffect, variant, icon, tag, rel, class, target, etc.)
    enable: true
    label: "Get In Touch"
    url: "/contact"
    # hoverEffect: "" # Optional: text-flip | creative-fill | magnetic | magnetic-text-flip
    # variant: "" # Optional: fill | outline | text | circle | white
    # rel: "" # Optional
    # target: "" # Optional
---
