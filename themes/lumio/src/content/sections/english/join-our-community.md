---
enable: true
badge: "// JOIN OUR COMMUNITY"
title: "We are trusted by over 4000+ clients. <br />Join them now and grow your business."
bgImage: "/images/join-community-shape.png"
button:
  # Refer to the `sharedButton` schema in `src/sections.schema.ts` for all available configuration options
  # (e.g., enable, label, url, hoverEffect, variant, icon, tag, rel, class, target, etc.)
  enable: true
  label: "Get Started"
  url: "/contact"
  tag: "a" # Optional: a | button
  icon: # Optional
    enable: true
    name: "ArrowUpRight" # Optional
    position: "left" # Optional: left | right
  # hoverEffect: "" # Optional: text-flip | creative-fill | magnetic | magnetic-text-flip
  variant: "white" # Optional: fill | outline | text | circle | white
  # rel: "" # Optional
  # target: "" # Optional
---
