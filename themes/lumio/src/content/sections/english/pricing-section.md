---
enable: true
badge: "// FLEXIBLE PRICING PLAN"
title: "Design faster at simple cost"
list:
  - enable: true
    name: "Basic Plan"
    description: "Nemo enim ipsam voluptatem an voluptas aspernatur aut odit aut fugit,"
    price:
      - value: 35
        prependValue: $
    features:
      - "5 PPC Campaigns"
      - "Support Management"
      - "Cyber Security"
      - "24/7 Remote Support"
      - "Unlimited Download Data"
    button:
      # Refer to the `sharedButton` schema in `src/sections.schema.ts` for all available configuration options
      # (e.g., enable, label, url, hoverEffect, variant, icon, tag, rel, class, target, etc.)
      enable: true
      label: "Start My Project Now"
      url: "/contact/"
      # hoverEffect: "" # Optional: text-flip | creative-fill | magnetic | magnetic-text-flip
      # variant: "" # Optional: fill | outline | text | circle | white
      # rel: "" # Optional
      # target: "" # Optional
  - enable: true
    name: "Standard Plan"
    description: "Nemo enim ipsam voluptatem an voluptas aspernatur aut odit aut fugit,"
    price:
      - value: 135
        prependValue: $
    features:
      - "5 PPC Campaigns"
      - "Support Management"
      - "Cyber Security"
      - "24/7 Remote Support"
      - "Unlimited Download Data"
    button:
      # Refer to the `sharedButton` schema in `src/sections.schema.ts` for all available configuration options
      # (e.g., enable, label, url, hoverEffect, variant, icon, tag, rel, class, target, etc.)
      enable: true
      label: "Start My Project Now"
      url: "/contact/"
      # hoverEffect: "" # Optional: text-flip | creative-fill | magnetic | magnetic-text-flip
      # variant: "" # Optional: fill | outline | text | circle | white
      # rel: "" # Optional
      # target: "" # Optional
  - enable: true
    name: "Premium Plan"
    description: "Nemo enim ipsam voluptatem an voluptas aspernatur aut odit aut fugit,"
    price:
      - value: 175
        prependValue: $
    features:
      - "5 PPC Campaigns"
      - "Support Management"
      - "Cyber Security"
      - "24/7 Remote Support"
      - "Unlimited Download Data"
    button:
      # Refer to the `sharedButton` schema in `src/sections.schema.ts` for all available configuration options
      # (e.g., enable, label, url, hoverEffect, variant, icon, tag, rel, class, target, etc.)
      enable: true
      label: "Start My Project Now"
      url: "/contact/"
      # hoverEffect: "" # Optional: text-flip | creative-fill | magnetic | magnetic-text-flip
      # variant: "" # Optional: fill | outline | text | circle | white
      # rel: "" # Optional
      # target: "" # Optional
---
