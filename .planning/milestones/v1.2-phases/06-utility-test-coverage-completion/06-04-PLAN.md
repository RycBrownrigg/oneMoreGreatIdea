---
phase: 06-utility-test-coverage-completion
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - themes/lumio/src/__tests__/getRelatedContent.test.ts
  - themes/lumio/src/__tests__/preline.test.ts
autonomous: true
requirements: [TECHDEBT-17, TECHDEBT-18]

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "getRelatedContent.ts and preline.ts each have a dedicated test file exercising real behavior (normal case plus at least one edge case), not smoke-only assertions."
    - "Running `npm --prefix themes/lumio run test:ci` after this plan still passes every suite from Plan 01 plus these 2 new suites, with zero regressions."
    - "Deliberately breaking either module's logic causes at least one new test in this plan to fail."
    - "After Plans 01-04 all land, every one of the 13 v1.2 requirement IDs (TECHDEBT-08 through TECHDEBT-20) has a corresponding passing test file, and the site's full complement of 19 pure-testable src/lib/utils/ modules (6 from v1.1 Phase 5 + these 13) has real regression coverage."
  artifacts:
    - themes/lumio/src/__tests__/getRelatedContent.test.ts
    - themes/lumio/src/__tests__/preline.test.ts
  key_links:
    - "Neither module imports `@/*`, `.astro/config.generated.json`, or `import.meta` — zero code dependency on Plan 01's harness fix; depends_on: [\"06-01\"] reflects phase-level tracer-first sequencing only."
  prohibitions:
    - "Must NOT modify getRelatedContent.ts or preline.ts themselves while writing their tests."
---

<objective>
Add real regression tests for the last 2 modules in this phase's scope: `getRelatedContent.ts` and `preline.ts`. This is the final plan of Phase 6 — once it lands, all 13 v1.2 requirements (TECHDEBT-08 through TECHDEBT-20) are closed and all 19 pure-testable `src/lib/utils/` modules (6 from v1.1 Phase 5, 13 from this phase) have dedicated Jest coverage.

Purpose: Close TECHDEBT-17 and TECHDEBT-18, completing Phase 6 and the v1.2 milestone.
Output: 2 new passing test files, zero regressions to any prior suite in this phase.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add getRelatedContent.test.ts</name>
  <files>themes/lumio/src/__tests__/getRelatedContent.test.ts</files>
  <read_first>themes/lumio/src/lib/utils/getRelatedContent.ts, themes/lumio/src/__tests__/sortFunctions.test.ts</read_first>
  <action>
Create `themes/lumio/src/__tests__/getRelatedContent.test.ts` importing the default export from `../lib/utils/getRelatedContent`. `getRelatedContent(contentList, currentContent, keysToConsider, reservedWords, limit)` excludes any item whose `data.title` equals `currentContent.data.title`, normalizes each considered key's text (lowercase, split on spaces, words longer than 4 chars, minus reserved words), counts matching-word overlap plus category overlap, and returns the top `limit` matches sorted by match count descending. Write 3 tests against real, observed output: (a) given a `currentContent` with `data: { title: "Current", categories: ["astro"], description: "building websites fast" }` and a `contentList` containing an item sharing the `"astro"` category and the word `"building"` (5+ letters), plus an unrelated item with a different category and no shared words, plus `currentContent` itself in the list — the result contains only the related item, excluding both the unrelated item (no match) and `currentContent` itself (title-equality exclusion), verified by direct execution; (b) an empty `contentList` returns `[]` without throwing; (c) the default `limit` of `3` is respected — construct a `contentList` with 4+ items that would all match, and assert the result array's length is capped at 3.
  </action>
  <verify>
    <automated>npm --prefix themes/lumio run test:ci</automated>
    <fails_when>non-zero exit code, or "failed" appears in the Jest summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - All 3 tests pass; test (a) explicitly asserts both the self-exclusion and the no-match-exclusion in the same test, proving real filtering logic rather than a smoke test.
  </acceptance_criteria>
  <done>getRelatedContent.ts has 3 passing tests covering category/word-overlap matching with self-exclusion, empty-list input, and the default result-limit cap.</done>
</task>

<task type="auto">
  <name>Task 2: Add preline.test.ts</name>
  <files>themes/lumio/src/__tests__/preline.test.ts</files>
  <read_first>themes/lumio/src/lib/utils/preline.ts, themes/lumio/src/__tests__/objectFunctions.test.ts</read_first>
  <action>
Create `themes/lumio/src/__tests__/preline.test.ts` importing the named export `getPrelineSelectConfig` from `../lib/utils/preline`. Despite the filename, this module is pure — it returns a plain configuration object (CSS classes and markup strings) for Preline's select component, with no DOM or `window` access, so it's directly unit-testable. Write 2 tests against real, observed output: (a) calling with only `{ placeholder: "Choose" }` (all other params defaulted) produces `hasSearch: false`, `searchPlaceholder: ""` (empty string, since `hasSearch` is falsy — the ternary `hasSearch ? searchPlaceholder || "Search..." : ""` short-circuits to `""` regardless of any `searchPlaceholder` argument when `hasSearch` is not passed), and `placeholder: "Choose"` passed through unchanged; (b) calling with `{ placeholder: "Choose", hasSearch: true }` (no `searchPlaceholder` given) produces `searchPlaceholder: "Search..."` — the same ternary's OTHER branch, falling back to the default text when `hasSearch` is true but no explicit placeholder was supplied, verified by direct execution.
  </action>
  <verify>
    <automated>npm --prefix themes/lumio run test:ci</automated>
    <fails_when>non-zero exit code, or "failed" appears in the Jest summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - All 2 tests pass, covering both branches of the `hasSearch ? searchPlaceholder || "Search..." : ""` ternary (the `false` branch forcing empty string regardless of input, and the `true`-with-no-override branch falling back to the default text).
  </acceptance_criteria>
  <done>preline.ts has 2 passing tests covering both branches of its hasSearch/searchPlaceholder logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | This plan adds Jest test files only, exercising pure, already-shipped functions with no I/O, auth, or network behavior. Test files are excluded from the Astro `dist/` build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-06-04 | N/A | Test-only files (`getRelatedContent.test.ts`, `preline.test.ts`) | low | accept | Dev-time only, never bundled into `dist/`. No auth, network, filesystem, or secret-handling code is touched — pure content-matching and config-object functions already shipped in prior milestones. |
</threat_model>

<verification>
- `npm --prefix themes/lumio run test:ci` exits 0 with all suites (Plan 01's plus this plan's 2) passing.
- `git diff --stat -- themes/lumio/src/lib/utils/` (from repo root) returns no output.
- Once Plans 01-04 have all landed: every module named in ROADMAP.md's Phase 6 success criteria #1 (generateTypeScale, filteredEnabled, handleDraftPage, overrideObjects, uniqueIdGenerator, removeEmptyKeys, readingTime, buildToc, navigationActive, getRelatedContent, preline, absoluteUrl, JsonLdGenerator) has a `src/__tests__/*.test.ts` file, and `npm --prefix themes/lumio run test:ci` reports 20 total suites (7 pre-existing + 13 new).
</verification>

<success_criteria>
getRelatedContent.ts and preline.ts each have real-behavior test coverage, with zero regressions to any prior suite. Phase 6 is complete: all 13 v1.2 requirements closed, all 19 pure-testable `src/lib/utils/` modules covered.
</success_criteria>

## Artifacts this phase produces (this plan's share)

- `themes/lumio/src/__tests__/getRelatedContent.test.ts` (new) — `describe("getRelatedContent")`, 3 tests
- `themes/lumio/src/__tests__/preline.test.ts` (new) — `describe("getPrelineSelectConfig")`, 2 tests

<output>
Create `.planning/phases/06-utility-test-coverage-completion/06-04-SUMMARY.md` when done
</output>
