---
phase: 06-utility-test-coverage-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - themes/lumio/tsconfig.jest.json
  - themes/lumio/jest.config.ts
  - themes/lumio/package.json
  - themes/lumio/src/__tests__/handleDraftPage.test.ts
  - themes/lumio/src/__tests__/buildToc.test.ts
  - themes/lumio/src/__tests__/navigationActive.test.ts
  - themes/lumio/src/__tests__/JsonLdGenerator.test.ts
  - themes/lumio/src/__tests__/absoluteUrl.test.ts
autonomous: true
requirements: [TECHDEBT-10, TECHDEBT-15, TECHDEBT-16, TECHDEBT-20, TECHDEBT-19]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Running `npm --prefix themes/lumio run test:ci` locally and in CI produces zero failures across all suites (the pre-existing 7 suites / 86 tests, plus every new suite this plan and Plans 02-04 add)."
    - "The Jest harness can resolve `@/*` path-alias imports, the bare `.astro/config.generated.json` specifier, and `import.meta` syntax used by production modules under test — all three are genuine pre-existing gaps in `tsconfig.jest.json`/`jest.config.ts`, empirically confirmed by direct experimentation before this plan was written (see Deviations note below), not assumptions."
    - "handleDraftPage.ts, buildToc.ts, navigationActive.ts, and JsonLdGenerator.ts each have a dedicated test file exercising real behavior (not smoke-only assertions), importable and runnable without any production-code change."
    - "No production module file's logic was altered to make it testable — only test-harness config (tsconfig.jest.json, jest.config.ts, package.json scripts) and new test files change."
    - "The existing `.github/workflows/ci.yml` picks up all new suites on the next push with zero workflow-file edits, because the harness fix lives entirely inside `package.json`'s `test:ci` script, which CI already invokes verbatim."
    - "Deliberately breaking handleDraftPage.ts's, buildToc.ts's, navigationActive.ts's, or JsonLdGenerator.ts's logic causes at least one new test in this plan to fail."
  artifacts:
    - themes/lumio/tsconfig.jest.json
    - themes/lumio/jest.config.ts
    - themes/lumio/package.json
    - themes/lumio/src/__tests__/handleDraftPage.test.ts
    - themes/lumio/src/__tests__/buildToc.test.ts
    - themes/lumio/src/__tests__/navigationActive.test.ts
    - themes/lumio/src/__tests__/JsonLdGenerator.test.ts
    - themes/lumio/src/__tests__/absoluteUrl.test.ts
  key_links:
    - "jest.config.ts's moduleNameMapper entries for `^@/(.*)$` and `^\\.astro/config\\.generated\\.json$` <-> handleDraftPage.ts/buildToc.ts/navigationActive.ts/JsonLdGenerator.ts's `@/` and bare `.astro/config.generated.json` imports — if either mapping is missing, those 4 suites fail to compile with TS2307/'Cannot find module', before any test runs."
    - "package.json's `NODE_OPTIONS=--experimental-vm-modules` on `test`/`test:ci` <-> textConverter.test.ts's `marked` import — enabling real ESM lets Jest resolve marked's native ESM build directly, so the OLD `^marked$` -> UMD-build moduleNameMapper entry must be REMOVED (not kept alongside the new mapping), or textConverter.test.ts's marked import will silently double-resolve incorrectly. This was empirically verified: keeping both real-ESM-mode and the UMD mapping breaks `textConverter.test.ts` with 'does not provide an export named marked'."
    - "tsconfig.jest.json's `paths: { \"@/*\": [\"./src/*\"] }` <-> ts-jest's per-file TypeScript diagnostics for `@/types`, `@/lib/utils/i18nUtils`, `@/config/social.json` — ts-jest type-checks each file against this tsconfig (it does NOT extend the project's own tsconfig.json), so without this entry those imports throw TS2307 even for type-only imports."
  assumptions:
    - "FLAGGED (specless-probe fallback, unresolved): none of the 13 TECHDEBT requirement texts ('Automated unit tests cover X.ts') carry explicit edge-case language, so the deterministic edge-probe classified all 13 as unclassified/unresolved. Per the spec-less fallback protocol this is surfaced explicitly rather than silently dropped: the exact edge cases each test file exercises are the executor's engineering judgment, guided by the v1.1 Phase 5 precedent (normal case + at least one edge case per export: empty/missing/malformed input) and by the concrete real-behavior values recorded in each task's <action> below, which were captured by running the actual code, not guessed."
  prohibitions:
    - "Must NOT modify the production module files themselves (handleDraftPage.ts, buildToc.ts, navigationActive.ts, JsonLdGenerator.ts, absoluteUrl.ts, or any of the other 8 modules covered in Plans 02-04) while writing their tests — only test files and test-harness config may change."
    - "Must NOT weaken or remove assertions in the existing 7 passing test suites (dateFormat, textConverter, sortFunctions, objectFunctions, splitProtectedText, trailingSlashChecker, getLocalUrlCTM) to accommodate the harness change in Task 1 — the harness change must make all 4 new suites pass AND keep all 86 existing assertions passing unmodified."
---

<objective>
Prove the Jest test harness can actually compile and run the 4 hardest-to-test modules in this phase's scope (handleDraftPage.ts, buildToc.ts, navigationActive.ts, JsonLdGenerator.ts — the ones that import `@/*` aliases, the bare `.astro/config.generated.json` specifier, or `import.meta.env`), by fixing the harness end-to-end and writing real tests against it, before Plans 02-04 fan out to the remaining 9 simpler modules.

This is the phase's tracer: the pure-function logic in all 13 target modules is trivial, but 4 of them expose genuine, pre-existing gaps in `tsconfig.jest.json`/`jest.config.ts` that make them uncompilable under the current harness — confirmed by direct hands-on experimentation while planning this phase (not a guess). Task 1 fixes that harness gap in one shot and proves it against the single hardest case (handleDraftPage.ts, which additionally requires real ESM mode for `import.meta` syntax to be legal at all). Tasks 2-3 spend that proven harness on the other 3 affected modules before Plans 02-04 touch the remaining 9.

Purpose: Close TECHDEBT-10, TECHDEBT-15, TECHDEBT-16, TECHDEBT-19, and TECHDEBT-20 with real regression tests, and leave a working, non-regressive Jest harness for the rest of the phase to build on.
Output: A working `@/*`/`.astro/config.generated.json`/`import.meta`-capable Jest harness (tsconfig.jest.json, jest.config.ts, package.json), plus 5 new passing test files, with zero regressions to the 86 pre-existing tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.1-phases/05-utility-test-coverage/05-01-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Fix the Jest harness for `@/*`/`.astro/config.generated.json`/`import.meta`, proven via handleDraftPage.test.ts</name>
  <files>themes/lumio/tsconfig.jest.json, themes/lumio/jest.config.ts, themes/lumio/package.json, themes/lumio/src/__tests__/handleDraftPage.test.ts</files>
  <read_first>themes/lumio/src/lib/utils/handleDraftPage.ts, themes/lumio/tsconfig.jest.json, themes/lumio/jest.config.ts, themes/lumio/package.json (scripts block), themes/lumio/src/__tests__/dateFormat.test.ts, themes/lumio/src/__tests__/sortFunctions.test.ts</read_first>
  <precondition>themes/lumio/.astro/config.generated.json must already exist (run `npm run toml:watch` once from themes/lumio/ if missing) — buildToc.ts and navigationActive.ts (Task 2) import it directly at module-load time, and its absence would surface as a confusing "Cannot find module" error unrelated to this task's actual fix.</precondition>
  <action>
Three pre-existing harness gaps block 4 of this phase's 13 modules from compiling under ts-jest, all empirically confirmed (not assumed) during phase planning by writing throwaway probe tests against each module and observing the actual failure. Fix all three in this one task, in this order, re-running `npm --prefix themes/lumio run test:ci` after each change to confirm no new failures before moving to the next:

1. In `themes/lumio/tsconfig.jest.json`'s `compilerOptions`, add three keys: `"module": "esnext"`, `"baseUrl": "."`, and `"paths": { "@/*": ["./src/*"] }`. Reasoning to preserve in a comment is not possible in JSON, so instead add these via the plan's own commit message context: `tsconfig.jest.json` does NOT `extend` the project's root `tsconfig.json`, so it currently has none of `@/*`'s path-alias mapping (causing ts-jest's per-file type-check to throw TS2307 "Cannot find module '@/types'" even on type-only imports in buildToc.ts and navigationActive.ts) and no explicit `"module"` (ts-jest's config-set defaults an unset `module` to CommonJS internally, which — due to a `??` nullish-coalescing quirk in ts-jest's `fixupCompilerOptionsForModuleKind` — silently overrides any attempt to run in ESM mode later, causing `import.meta` in handleDraftPage.ts to fail parsing with TS1343 regardless of Jest's own ESM flag).
2. In `themes/lumio/jest.config.ts`, replace the existing `moduleNameMapper` object (currently just `"^marked$": "<rootDir>/node_modules/marked/lib/marked.umd.js"`) with three entries: `"^@/(.*)$": "<rootDir>/src/$1"` (runtime resolution for the alias added to tsconfig in step 1 — ts-jest only uses tsconfig `paths` for type-checking, not actual module resolution, so this second mapping is required too), `"^\\.astro/config\\.generated\\.json$": "<rootDir>/.astro/config.generated.json"` (buildToc.ts imports this exact bare, non-relative specifier verbatim — mirroring how astro.config.mjs and the site's own `.astro` components import it — and Node's resolver cannot resolve a bare specifier starting with a literal dot on its own). Remove the `^marked$` UMD-build mapping entirely — once step 3 below enables real ESM mode, Jest resolves `marked`'s native ESM build directly and keeping the old UMD mapping causes `textConverter.test.ts` to fail with "does not provide an export named 'marked'" (empirically confirmed). Update the file's leading comment to explain the new mappings and why the old one is gone, matching this file's existing pattern of prose comments above `moduleNameMapper`.
3. In `themes/lumio/package.json`'s `scripts`, prepend `NODE_OPTIONS=--experimental-vm-modules` (as an additional `cross-env`-style `KEY=value` pair, alongside the existing `TS_NODE_PROJECT=...`) to both the `test` and `test:ci` script values. This is the piece that actually makes `import.meta` legal at runtime: Jest's `ts-jest` ESM preset only reports `supportsStaticESM: true` to the transformer (which is required for `import.meta` to parse and for `module: esnext` to actually take effect per-file) when Jest itself is running under Node's real ESM VM-modules mode, which requires this flag — without it, ts-jest silently falls back to compiling every file as CommonJS regardless of tsconfig, which is exactly why steps 1-2 alone are insufficient.
4. Create `themes/lumio/src/__tests__/handleDraftPage.test.ts` importing the default export from `../lib/utils/handleDraftPage`. Write two tests against `handleDraftPage`'s real, observed behavior under this harness (confirmed by direct experimentation, not inferred from reading the source alone): (a) `handleDraftPage({ draft: false })` returns `undefined` — the `pageData.draft && import.meta.env.PROD` check short-circuits before touching `import.meta.env`, so this path never depends on Astro's build-time env injection; (b) `handleDraftPage({ draft: true })` THROWS (assert with `expect(() => ...).toThrow()`) — under Jest, `import.meta.env` is `undefined` (Vite/Astro's build-time env injection never runs here), so accessing `.PROD` on it throws `TypeError: Cannot read properties of undefined (reading 'PROD')`; this is real, observed harness behavior, not the production 404-Response behavior this function has inside an actual Astro build — document that distinction in a comment above the test, mirroring `trailingSlashChecker.test.ts`'s existing pattern of a comment explaining a value that's environment-dependent.
  </action>
  <reversibility rating="reversible">Harness config and new test files are fully revertible via `git revert`; no production module logic or shipped `dist/` output changes. The real-ESM-mode choice (over alternatives like babel-transform overrides or skipping handleDraftPage.ts's true-branch) was the only approach that worked without also requiring a second, larger fix to marked/textConverter — documented above so a future revert-and-reconsider is informed, not blind.</reversibility>
  <verify>
    <automated>npm --prefix themes/lumio run test:ci</automated>
    <fails_when>non-zero exit code, or "failed" appears in the Jest summary line (e.g. "Tests: N failed")</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm --prefix themes/lumio run test:ci` exits 0 with "Test Suites: 8 passed, 8 total" and total test count of 88 or more (86 pre-existing + the 2 new handleDraftPage cases), with zero suites failing to compile.
    - `themes/lumio/src/__tests__/handleDraftPage.test.ts` exists with exactly the two behaviors described above, both passing.
    - `git diff --stat -- themes/lumio/src/lib/utils/` (from repo root) returns no output — no production module file was touched.
  </acceptance_criteria>
  <done>The Jest harness resolves `@/*`, the bare `.astro/config.generated.json` specifier, and `import.meta` syntax; handleDraftPage.ts has 2 passing tests documenting its real (harness-observed) behavior; all 86 pre-existing tests still pass unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Add buildToc.test.ts and navigationActive.test.ts, spending the harness fix from Task 1</name>
  <files>themes/lumio/src/__tests__/buildToc.test.ts, themes/lumio/src/__tests__/navigationActive.test.ts</files>
  <read_first>themes/lumio/src/lib/utils/buildToc.ts, themes/lumio/src/lib/utils/navigationActive.ts, themes/lumio/src/__tests__/sortFunctions.test.ts, themes/lumio/.astro/config.generated.json (to confirm the live `settings.markup.tableOfContents` and `site.baseUrl` values these tests assert against)</read_first>
  <action>
Create `themes/lumio/src/__tests__/buildToc.test.ts` importing the default export from `../lib/utils/buildToc`. `buildToc` reads `config.settings.markup.tableOfContents` (`startLevel`, `endLevel`, `ordered`) from the real generated config at import time — confirm the live values first (empirically observed as `startLevel: 1, endLevel: 5, ordered: false` at planning time; re-confirm from the file you just read, since this project regenerates it from `config.toml` and it can change) and write assertions relative to those real values, mirroring `trailingSlashChecker.test.ts`'s existing pattern of asserting against a live config value rather than a hardcoded assumption. Write 3 tests: (a) nesting — given a `depth: 2` heading followed by a `depth: 3` heading followed by another `depth: 2` heading, the second depth-3 heading nests under the first depth-2 heading as a `subheadings` entry, and the second depth-2 heading becomes a second top-level sibling (empirically confirmed output shape: two top-level entries, the first with one `subheadings` child) — with `ordered: false` the two top-level entries preserve insertion order, NOT alphabetical order (do not assert alphabetical sorting); (b) a heading whose `depth` falls outside `[startLevel, endLevel]` (e.g. `depth: 6` when `endLevel: 5`) produces an empty result array; (c) an empty `headings` array input produces an empty result array without throwing.
Create `themes/lumio/src/__tests__/navigationActive.test.ts` importing the named exports `isActiveUrl` and `isActiveMenu` from `../lib/utils/navigationActive`. Write 4 tests against real, observed behavior: (a) `isActiveUrl` given an absolute external URL (e.g. `"https://external.com/foo"`) with any `currentPath` returns `false` — external URLs never match, verified against the real `site.baseUrl` in the generated config; (b) `isActiveUrl` given `undefined` as the `url` argument returns `false` without throwing; (c) `isActiveMenu` given a menu item with `enable: false` and a `url` that exactly matches `currentPath` still returns `false` — the enabled-check short-circuits before the URL match; (d) `isActiveMenu` recurses into a `children` array — a parent item whose own `url` does not match `currentPath` still returns `true` when one of its `children` items' `url` does match, and returns `false` when neither the parent nor any child matches.
  </action>
  <verify>
    <automated>npm --prefix themes/lumio run test:ci</automated>
    <fails_when>non-zero exit code, or "failed" appears in the Jest summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm --prefix themes/lumio run test:ci` exits 0 with "Test Suites: 9 passed, 9 total" (adds 1 suite on top of Task 1's 8) and a total test count that grew by 7 (3 buildToc + 4 navigationActive) relative to Task 1's count.
    - Both new test files import from the real production modules (no mocking of `buildToc`/`navigationActive` themselves) and assert against the live `.astro/config.generated.json` values read in `read_first`, not hardcoded guesses.
  </acceptance_criteria>
  <done>buildToc.ts and navigationActive.ts each have real-behavior tests (nesting/range/empty for buildToc; external-URL/undefined/disabled/recursion for navigationActive), all passing, with zero regressions to Task 1's 88 tests.</done>
</task>

<task type="auto">
  <name>Task 3: Add JsonLdGenerator.test.ts and absoluteUrl.test.ts</name>
  <files>themes/lumio/src/__tests__/JsonLdGenerator.test.ts, themes/lumio/src/__tests__/absoluteUrl.test.ts</files>
  <read_first>themes/lumio/src/lib/utils/JsonLdGenerator.ts, themes/lumio/src/lib/utils/absoluteUrl.ts, themes/lumio/src/config/social.json, themes/lumio/src/__tests__/objectFunctions.test.ts</read_first>
  <action>
Create `themes/lumio/src/__tests__/absoluteUrl.test.ts` importing the named export `absoluteUrl` from `../lib/utils/absoluteUrl`. `absoluteUrl(url, Astro)` reads `Astro.url.href` and resolves `url` against it via the `URL` constructor — no real Astro object is needed, a plain object literal `{ url: new URL("https://example.com/base/") }` is sufficient (this was verified directly: `absoluteUrl("/logo.png", { url: new URL("https://example.com/base/") })` returns exactly `"https://example.com/logo.png"`, root-relative resolution, not relative to the `/base/` path segment). Write 2 tests: (a) a root-relative path resolves against the fake `Astro.url.href`'s origin as shown above; (b) an already-absolute input URL (e.g. `"https://other.com/x"`) passes through unchanged (verify this is `absoluteUrl`'s real behavior by reasoning from the `new URL(url, base).href` call — an absolute `url` argument ignores the `base` entirely per the `URL` constructor's own spec, which you can confirm with a one-line manual check before committing to the assertion).
Create `themes/lumio/src/__tests__/JsonLdGenerator.test.ts` importing the default export `JsonLdGenerator` from `../lib/utils/JsonLdGenerator`. This module needs both a fake `Astro` object (`{ url: new URL("https://example.com/about"), currentLocale: "en" }` is sufficient) and a `config` object passed as part of its `content` argument (a real production `config.generated.json` shape works, or a minimal fake with `settings.multilingual.defaultLanguage`, `site.title`, `site.tagline`, `site.taglineSeparator`, `site.description`, `site.logo`, and `seo.author` — all fields the function reads). Write 2 tests against real, observed behavior: (a) calling with `{ canonical: "/about", title: "About", config: <fake config> }` and the fake Astro produces a result whose `"@type"` is `"WebPage"`, whose `isPartOf.url` equals the trailing-slash-normalized `Astro.url.origin`, and whose `publisher.logo.url` is the result of resolving `config.site.logo` through `absoluteUrl` (i.e. an absolute URL, not the bare `config.site.logo` path) — this exercises the real cross-module wiring to `absoluteUrl` and `trailingSlashChecker` rather than mocking them; (b) omitting `lang` from the input `content` falls back to `config.settings.multilingual.defaultLanguage` (assert the result's `inLanguage` field equals that fallback value, not `undefined`).
  </action>
  <verify>
    <automated>npm --prefix themes/lumio run test:ci</automated>
    <fails_when>non-zero exit code, or "failed" appears in the Jest summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm --prefix themes/lumio run test:ci` exits 0 with "Test Suites: 11 passed, 11 total" (adds 2 suites on top of Task 2's 9) and a total test count that grew by 4 (2 absoluteUrl + 2 JsonLdGenerator) relative to Task 2's count.
    - `JsonLdGenerator.test.ts` does not mock `absoluteUrl` or `trailingSlashChecker` — it exercises the real cross-module calls JsonLdGenerator.ts makes to them.
  </acceptance_criteria>
  <done>absoluteUrl.ts and JsonLdGenerator.ts each have real-behavior tests, all passing, with zero regressions to Task 2's 95 tests. This plan's 5 modules (handleDraftPage, buildToc, navigationActive, JsonLdGenerator, absoluteUrl) are fully covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | This plan adds Jest test files under `src/__tests__/` (executed only via `npm run test:ci`, never bundled into the static `dist/` build Astro produces) and edits test-harness config (`tsconfig.jest.json`, `jest.config.ts`, `package.json` scripts). No user input, network call, authentication, or persisted data is introduced or touched by anything this plan changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-06-01 | N/A | Test-only files (`src/__tests__/*.test.ts`) and harness config (`jest.config.ts`, `tsconfig.jest.json`, `package.json` scripts) | low | accept | These files are dev-time only, excluded from the Astro build output (`dist/`) that is rsync'd to the VPS, and never served to a client. No ASVS L1 control applies: no auth, network I/O, filesystem I/O, or secret-handling code is touched — this plan adds regression tests for already-shipped pure functions (handleDraftPage, buildToc, navigationActive, JsonLdGenerator, absoluteUrl) and fixes a test-only module-resolution gap. Real production risk (if any) already existed prior to this phase and is unchanged by it. |
</threat_model>

<verification>
- `npm --prefix themes/lumio run test:ci` exits 0, ending with "Test Suites: 11 passed, 11 total" and a total test count of at least 92 (86 pre-existing + 6 new: 2 handleDraftPage + 3 buildToc + 4 navigationActive... recount precisely from the actual Jest summary at execution time, since exact per-test counts depend on final assertions written).
- `npm --prefix themes/lumio run astro-check` still reports "0 errors, 0 warnings, 0 hints" — the tsconfig.jest.json change is isolated to the Jest-only config file and must not affect the main `tsconfig.json`-driven Astro type-check.
- `git diff --stat -- themes/lumio/src/lib/utils/` (from repo root) returns no output — confirms no production module logic was altered.
</verification>

<success_criteria>
- All 5 of this plan's modules (handleDraftPage.ts, buildToc.ts, navigationActive.ts, JsonLdGenerator.ts, absoluteUrl.ts) have dedicated, passing test files exercising real (not smoke-only) behavior.
- The Jest harness fix (tsconfig.jest.json + jest.config.ts + package.json) is proven stable: zero regressions to the 86 pre-existing tests, and `.github/workflows/ci.yml` needs no edits since it already invokes `npm run test:ci` verbatim.
- Plans 02, 03, and 04 (Wave 2) can proceed against this proven harness without re-deriving any of it.
</success_criteria>

## Artifacts this phase produces (this plan's share)

- `themes/lumio/src/__tests__/handleDraftPage.test.ts` (new) — `describe("handleDraftPage")` suite, 2 tests
- `themes/lumio/src/__tests__/buildToc.test.ts` (new) — `describe("buildToc")` suite, 3 tests
- `themes/lumio/src/__tests__/navigationActive.test.ts` (new) — `describe("navigationActive")` suite (or split `isActiveUrl`/`isActiveMenu` describes), 4 tests
- `themes/lumio/src/__tests__/JsonLdGenerator.test.ts` (new) — `describe("JsonLdGenerator")` suite, 2 tests
- `themes/lumio/src/__tests__/absoluteUrl.test.ts` (new) — `describe("absoluteUrl")` suite, 2 tests
- `themes/lumio/tsconfig.jest.json` (modified) — adds `module: "esnext"`, `baseUrl: "."`, `paths: { "@/*": ["./src/*"] }`
- `themes/lumio/jest.config.ts` (modified) — `moduleNameMapper` gains `^@/(.*)$` and `^\.astro/config\.generated\.json$`, loses `^marked$`
- `themes/lumio/package.json` (modified) — `test` and `test:ci` scripts gain `NODE_OPTIONS=--experimental-vm-modules`

<output>
Create `.planning/phases/06-utility-test-coverage-completion/06-01-SUMMARY.md` when done
</output>
