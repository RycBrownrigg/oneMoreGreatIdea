---
phase: 06-utility-test-coverage-completion
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - themes/lumio/src/__tests__/overrideObjects.test.ts
  - themes/lumio/src/__tests__/removeEmptyKeys.test.ts
  - themes/lumio/src/__tests__/uniqueIdGenerator.test.ts
autonomous: true
requirements: [TECHDEBT-11, TECHDEBT-13, TECHDEBT-12]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "overrideObjects.ts, removeEmptyKeys.ts, and uniqueIdGenerator.ts each have a dedicated test file exercising real behavior (normal case plus at least one edge case), not smoke-only assertions."
    - "Running `npm --prefix themes/lumio run test:ci` after this plan still passes every suite from Plan 01 plus these 3 new suites, with zero regressions."
    - "Deliberately breaking any one of these 3 modules' logic causes at least one new test in this plan to fail."
  artifacts:
    - themes/lumio/src/__tests__/overrideObjects.test.ts
    - themes/lumio/src/__tests__/removeEmptyKeys.test.ts
    - themes/lumio/src/__tests__/uniqueIdGenerator.test.ts
  key_links:
    - "overrideObjects.ts imports recursiveCloneObject from ./objectFunctions (already tested in v1.1 Phase 5) — this plan's test exercises that real cross-module call rather than mocking it, so a regression in recursiveCloneObject's clone-independence guarantee would also surface here."
    - "None of these 3 modules import `@/*`, `.astro/config.generated.json`, or `import.meta` — zero code dependency on Plan 01's harness fix; depends_on: [\"06-01\"] reflects phase-level tracer-first sequencing only."
  prohibitions:
    - "Must NOT modify overrideObjects.ts, removeEmptyKeys.ts, or uniqueIdGenerator.ts themselves while writing their tests (this includes objectFunctions.ts, which overrideObjects.ts depends on and which already has its own test file from v1.1 Phase 5)."
---

<objective>
Add real regression tests for the 3 remaining Data/Object utilities: `overrideObjects.ts`, `removeEmptyKeys.ts`, and `uniqueIdGenerator.ts`.

Purpose: Close TECHDEBT-11, TECHDEBT-12, and TECHDEBT-13.
Output: 3 new passing test files, zero regressions to Plan 01's suites.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add overrideObjects.test.ts</name>
  <files>themes/lumio/src/__tests__/overrideObjects.test.ts</files>
  <read_first>themes/lumio/src/lib/utils/overrideObjects.ts, themes/lumio/src/lib/utils/objectFunctions.ts, themes/lumio/src/__tests__/objectFunctions.test.ts</read_first>
  <action>
Create `themes/lumio/src/__tests__/overrideObjects.test.ts` importing the default export from `../lib/utils/overrideObjects`. `overrideObjects(target, source)` deep-clones `target` (via `recursiveCloneObject`), then for each key in `source`: if both the target's and source's values at that key are non-array objects, recursively merges them; otherwise the source value overwrites the target value wholesale (this means array values are NEVER merged — a source array always replaces a target array entirely, even if both are non-empty). Write 3 tests against real, observed output: (a) merging `{ a: 1, nested: { x: 1, y: 2 } }` with `{ nested: { y: 99 } }` produces `{ a: 1, nested: { x: 1, y: 99 } }` — `x` is preserved from target, `y` is overwritten, proving the recursive-merge branch; (b) merging `{ list: [1, 2, 3] }` with `{ list: [9] }` produces `{ list: [9] }` — the target array is wholly replaced, not concatenated or merged element-by-element, a real and non-obvious edge case worth asserting explicitly; (c) calling `overrideObjects(target, source)` does not mutate the original `target` object (assert the original `target` reference is unchanged after the call), proving the initial `recursiveCloneObject` clone is real and independent.
  </action>
  <verify>
    <automated>npm --prefix themes/lumio run test:ci</automated>
    <fails_when>non-zero exit code, or "failed" appears in the Jest summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - All 3 tests pass; test (b) explicitly asserts wholesale array replacement (not merge/concat), and test (c) explicitly re-reads the original `target` variable after the call to prove non-mutation.
  </acceptance_criteria>
  <done>overrideObjects.ts has 3 passing tests covering deep-merge of nested objects, wholesale array replacement, and target non-mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Add removeEmptyKeys.test.ts</name>
  <files>themes/lumio/src/__tests__/removeEmptyKeys.test.ts</files>
  <read_first>themes/lumio/src/lib/utils/removeEmptyKeys.ts, themes/lumio/src/__tests__/sortFunctions.test.ts</read_first>
  <action>
Create `themes/lumio/src/__tests__/removeEmptyKeys.test.ts` importing the default export from `../lib/utils/removeEmptyKeys`. `removeEmptyKeys(obj)` mutates and returns `obj`, deleting any key whose value is `""` or an empty array (`[]`) at the top level, and recursing into any other object-typed (including array-typed, since `typeof [] === "object"` and arrays are NOT excluded by the recursive branch's guard) value, deleting that key afterward if the nested value became an empty object (`{}`, i.e. `Object.keys(...).length === 0`) as a result. `null` values are left untouched (neither deleted nor recursed into, because the recursive branch explicitly guards `value !== null`). Write 3 tests against real, observed output: (a) given `{ a: "", b: [], c: {}, d: null, e: "keep", f: { g: "" } }`, the result is exactly `{ d: null, e: "keep" }` — `a` and `b` are deleted for being empty-string/empty-array, `c` is deleted because it's an empty object even though it isn't caught by the (dead) first-branch check for empty objects, `d` (null) survives untouched, `e` survives as a non-empty string, and `f` is deleted because after recursing into it, `g`'s empty string is stripped leaving `f` itself empty — verified by direct execution, this exact result shape is real, not inferred; (b) a plain object with no empty values at all is returned unchanged; (c) calling with an already-empty object `{}` returns `{}` without throwing.
  </action>
  <verify>
    <automated>npm --prefix themes/lumio run test:ci</automated>
    <fails_when>non-zero exit code, or "failed" appears in the Jest summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - All 3 tests pass; test (a) asserts the exact real result `{ d: null, e: "keep" }`, proving the test reflects actual observed recursive-deletion behavior (including the null-survives and nested-empty-object-deletion edge cases) rather than a guess.
  </acceptance_criteria>
  <done>removeEmptyKeys.ts has 3 passing tests covering top-level empty-string/empty-array/empty-object deletion, null preservation, recursive nested-object cleanup, and the no-empty-values/empty-input edge cases.</done>
</task>

<task type="auto">
  <name>Task 3: Add uniqueIdGenerator.test.ts</name>
  <files>themes/lumio/src/__tests__/uniqueIdGenerator.test.ts</files>
  <read_first>themes/lumio/src/lib/utils/uniqueIdGenerator.ts, themes/lumio/src/__tests__/objectFunctions.test.ts</read_first>
  <action>
Create `themes/lumio/src/__tests__/uniqueIdGenerator.test.ts` importing the default export from `../lib/utils/uniqueIdGenerator`. `uniqueIdGenerator(str)` maintains a module-level `Map` cache keyed by the exact input `str`: on a cache miss it Fisher-Yates-shuffles `str`'s words (using `Math.random()`, so the shuffle itself is non-deterministic across process runs) and hashes the shuffled result into a base-36 string, then caches and returns that id; on a cache hit (same `str` seen before, within the same test-process lifetime) it returns the exact same cached id every time — this caching is the function's actual reason for existing (its docstring-free purpose is stable, repeatable ids per distinct input string within a session) and is what these tests should prove, not the internal hash algorithm's exact output value (which is legitimately non-deterministic between separate test runs due to the random shuffle, so do NOT hardcode an expected id string). Write 2 tests against real, observed behavior: (a) calling `uniqueIdGenerator("Hello World Test Input")` twice in the same test returns the identical string both times (assert equality between the two calls' return values, not against a hardcoded literal) — proving the cache-hit path is real; (b) calling with an empty string `""` does not throw and returns a defined string value (verified by direct execution: `""` is a valid, non-crashing input, producing `"0"` in the current implementation, though asserting only `typeof result === "string"` is safer than hardcoding `"0"` given the hash algorithm could shift).
  </action>
  <verify>
    <automated>npm --prefix themes/lumio run test:ci</automated>
    <fails_when>non-zero exit code, or "failed" appears in the Jest summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - All 2 tests pass; test (a) asserts equality between two same-input calls rather than a hardcoded id literal, correctly treating the underlying shuffle/hash as non-deterministic while still proving the caching contract.
  </acceptance_criteria>
  <done>uniqueIdGenerator.ts has 2 passing tests proving its cache-hit-returns-same-id contract and its no-crash-on-empty-string behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | This plan adds Jest test files only, exercising pure, already-shipped functions with no I/O, auth, or network behavior. Test files are excluded from the Astro `dist/` build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-06-03 | N/A | Test-only files (`overrideObjects.test.ts`, `removeEmptyKeys.test.ts`, `uniqueIdGenerator.test.ts`) | low | accept | Dev-time only, never bundled into `dist/`. No auth, network, filesystem, or secret-handling code is touched — pure object/string transforms already shipped in prior milestones. |
</threat_model>

<verification>
- `npm --prefix themes/lumio run test:ci` exits 0 with all suites (Plan 01's plus these 3) passing.
- `git diff --stat -- themes/lumio/src/lib/utils/` (from repo root) returns no output.
</verification>

<success_criteria>
overrideObjects.ts, removeEmptyKeys.ts, and uniqueIdGenerator.ts each have real-behavior test coverage, with zero regressions to any prior suite.
</success_criteria>

## Artifacts this phase produces (this plan's share)

- `themes/lumio/src/__tests__/overrideObjects.test.ts` (new) — `describe("overrideObjects")`, 3 tests
- `themes/lumio/src/__tests__/removeEmptyKeys.test.ts` (new) — `describe("removeEmptyKeys")`, 3 tests
- `themes/lumio/src/__tests__/uniqueIdGenerator.test.ts` (new) — `describe("uniqueIdGenerator")`, 2 tests

<output>
Create `.planning/phases/06-utility-test-coverage-completion/06-03-SUMMARY.md` when done
</output>
