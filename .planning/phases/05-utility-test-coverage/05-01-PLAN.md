---
phase: 05-utility-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - themes/lumio/src/__tests__/dateFormat.test.ts
  - themes/lumio/src/__tests__/textConverter.test.ts
  - themes/lumio/src/__tests__/sortFunctions.test.ts
  - themes/lumio/src/__tests__/objectFunctions.test.ts
  - themes/lumio/src/__tests__/splitProtectedText.test.ts
  - themes/lumio/src/__tests__/trailingSlashChecker.test.ts
autonomous: true
requirements: [TECHDEBT-06]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low        # no calibration samples on record; factor 1.0 applied

must_haves:
  truths:
    - "npm run test:ci passes a suite covering dateFormat.ts, textConverter.ts, sortFunctions.ts, objectFunctions.ts, splitProtectedText.ts, and trailingSlashChecker.ts, beyond the single pre-existing getLocalUrlCTM.test.ts"
    - "Each of the 6 covered modules has at least one normal-behavior test and at least one edge-case test (empty string, missing/undefined input, or malformed value)"
    - "Every module in src/lib/utils/ (27 total, minus the 6 covered here) is accounted for with a one-line exclusion reason recorded in this plan's SUMMARY.md — not silently skipped"
    - "Deliberately breaking one covered utility's implementation makes at least one new test fail, proving the tests assert behavior rather than merely execute code (reverted afterwards)"
    - "The existing GitHub Actions workflow (.github/workflows/ci.yml) picks up these new test files and runs them on push with zero workflow-file edits"
  artifacts:
    - themes/lumio/src/__tests__/dateFormat.test.ts
    - themes/lumio/src/__tests__/textConverter.test.ts
    - themes/lumio/src/__tests__/sortFunctions.test.ts
    - themes/lumio/src/__tests__/objectFunctions.test.ts
    - themes/lumio/src/__tests__/splitProtectedText.test.ts
    - themes/lumio/src/__tests__/trailingSlashChecker.test.ts
  key_links:
    - "trailingSlashChecker.ts imports .astro/config.generated.json directly (site.trailingSlash), the same pattern getLocalUrlCTM.test.ts already relies on successfully — no new test-environment setup needed"
    - "jest.config.ts uses ts-jest's ESM preset via tsconfig.jest.json, which Phase 4 fixed to include .astro/types.d.ts — new test files inherit that fix automatically, no per-file config needed"
    - "slugify and marked (textConverter.ts's dependencies) are already devDependencies and resolve cleanly under the existing Jest/ts-jest setup — confirmed during planning, no new dependency install needed"
    - "The CI workflow's test:ci step has no file-glob restriction — Jest's default testMatch picks up any *.test.ts under src/__tests__/ automatically, so success criterion 5 (no workflow-file edits) is a property of the existing setup, not something this plan needs to configure"
---

<objective>
Give the site's six highest-named-risk pure utility functions (date formatting, slug/text conversion, sorting, object cloning, protected-text splitting, and URL trailing-slash handling) real regression tests, enforced by the Phase 4 CI pipeline, and produce an honest, recorded accounting of every other module in `src/lib/utils/` — tested here, already covered elsewhere, structurally untestable outside Astro/DOM/filesystem, or deliberately deferred.

Purpose: close TECHDEBT-06. Phase 4 proved the CI gate genuinely gates; this phase gives it something substantial to gate on beyond the single pre-existing i18n URL test.

Output: 6 new test files under `themes/lumio/src/__tests__/`, each covering normal behavior plus at least one edge case; a full per-module accounting in this plan's SUMMARY.md (must_haves truth #3); a proven-in-CI green run.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ci-pipeline-deploy-config-cleanup/04-01-SUMMARY.md

@themes/lumio/src/lib/utils/dateFormat.ts
@themes/lumio/src/lib/utils/textConverter.ts
@themes/lumio/src/lib/utils/sortFunctions.ts
@themes/lumio/src/lib/utils/objectFunctions.ts
@themes/lumio/src/lib/utils/splitProtectedText.ts
@themes/lumio/src/lib/utils/trailingSlashChecker.ts
@themes/lumio/src/__tests__/getLocalUrlCTM.test.ts
</context>

<operating_rules>
- Every `npm` command in this plan runs from `themes/lumio/`, never the repo root.
- NEVER run `npm run dev`. NEVER run `npm run test` (the `--watch` variant never exits). Use `npm run test:ci` exclusively.
- This plan pushes directly to `origin main`, matching Phase 4's precedent. Confirm `git remote -v` shows `origin` → `https://github.com/RycBrownrigg/oneMoreGreatIdea.git` before pushing.
- Use `gh run list` / `gh run watch <id> --exit-status --repo RycBrownrigg/oneMoreGreatIdea` to prove real CI runs, per Phase 4's established pattern. Do not simulate.
- Any deliberate-breakage probe (for the mutation-check must_have) must be done on a local, uncommitted change only — never pushed — and reverted with a `git diff --stat` confirmation of zero diff before the real commit.
</operating_rules>

<tasks>

<task type="tracer">
  <name>Task 1: Prove the pattern end-to-end with dateFormat.test.ts, verified by a real CI run</name>
  <files>themes/lumio/src/__tests__/dateFormat.test.ts</files>
  <precondition>`npm run test:ci` from `themes/lumio/` currently exits 0 (Phase 4's baseline — 43/43 passing). Confirm before adding anything, so any new failure is attributable to this task's own test, not a pre-existing regression.</precondition>
  <reversibility rating="reversible">A new, additive test file — `git revert` removes it cleanly with no effect on any other file.</reversibility>
  <read_first>
    - themes/lumio/src/lib/utils/dateFormat.ts — the full function: two supported patterns (`"dd MMM, yyyy"` default, `"dd MMMM, yyyy"`), and a `throw` for any other pattern
    - themes/lumio/src/__tests__/getLocalUrlCTM.test.ts — the existing test file's import style and `describe`/`test` structure to match
    - themes/lumio/jest.config.ts — confirms the ts-jest ESM preset in use; no per-file config needed
  </read_first>
  <action>
    Create `themes/lumio/src/__tests__/dateFormat.test.ts`. Import the default export from `../lib/utils/dateFormat`. Cover:

    1. Default pattern (`"dd MMM, yyyy"`, no second argument) against a fixed known date (e.g. `new Date("2026-01-07")`) — assert the exact expected string (`"07 Jan, 2026"` — verify the precise `Intl.DateTimeFormat("en-US", {day:"2-digit",month:"short",year:"numeric"})` output for this date by running it in a scratch Node REPL before hardcoding the assertion, since locale formatting details like comma placement are easy to get subtly wrong).
    2. The `"dd MMMM, yyyy"` pattern against the same fixed date — assert the exact uppercase-month format (e.g. `"07 JANUARY, 2026"`), again verified against real `Intl.DateTimeFormat` output before hardcoding.
    3. A string date input (e.g. `"2026-01-07"`) instead of a `Date` object — both patterns must produce identical output to the `Date`-object case, since the function does `new Date(date)` internally regardless of input type. This is the edge case: input-type flexibility.
    4. An unsupported pattern string (e.g. `"yyyy/mm/dd"`) — assert the function throws, matching `Unsupported pattern: yyyy/mm/dd` (use `expect(() => dateFormat(...)).toThrow(...)`). This is the second edge case: invalid input.

    Do not guess at exact formatted strings — compute them for real (a throwaway `node -e "..."` check against the actual `Intl.DateTimeFormat` calls) before writing the assertion, then delete the scratch check. A wrong hardcoded string fails the CI run this task itself needs to prove green, so get it right the first time rather than iterating in CI.

    After the local verify passes, commit (`git add themes/lumio/src/__tests__/dateFormat.test.ts && git commit -m "test: add dateFormat.ts coverage (TECHDEBT-06)"`) and push to `origin main`. Poll `gh run list --repo RycBrownrigg/oneMoreGreatIdea --branch main --limit 1 --json databaseId,status,conclusion,headSha` until `headSha` matches this commit and `status` is `completed`; confirm `conclusion` is `success` via `gh run watch <id> --exit-status --repo RycBrownrigg/oneMoreGreatIdea`. Do not proceed to Task 2 until this real run is green — Task 2 assumes the pattern (new `*.test.ts` files under `src/__tests__/` are picked up by CI with zero workflow changes) is proven, not assumed.
  </action>
  <verify>
    <automated>cd /Users/ryc/projects/lumio2/themes/lumio && npm run test:ci > /tmp/gsd-phase05-task1.log 2>&1; echo "exit=$?"; grep -c "dateFormat" /tmp/gsd-phase05-task1.log</automated>
    <fails_when>`exit=` is anything other than `exit=0`, or the grep count is `0` (the new test file's suite name never appeared, meaning Jest did not pick it up)</fails_when>
  </verify>
  <acceptance_criteria>
    - `themes/lumio/src/__tests__/dateFormat.test.ts` exists with at least 4 test cases: default pattern, alternate pattern, string-vs-Date-object input equivalence, and an unsupported-pattern throw
    - `npm run test:ci` exits 0 with the new suite included in the total test count
    - Every hardcoded expected string was verified against real `Intl.DateTimeFormat` output before being committed, not guessed
  </acceptance_criteria>
  <done>`dateFormat.test.ts` exists, passes locally, and a real GitHub Actions run triggered by this task's own push to `origin main` completes with `conclusion: success`.</done>
</task>

<task type="auto">
  <name>Task 2: Cover the remaining 5 named modules, record the full per-module accounting, prove a deliberate breakage is caught, final CI proof</name>
  <files>themes/lumio/src/__tests__/textConverter.test.ts, themes/lumio/src/__tests__/sortFunctions.test.ts, themes/lumio/src/__tests__/objectFunctions.test.ts, themes/lumio/src/__tests__/splitProtectedText.test.ts, themes/lumio/src/__tests__/trailingSlashChecker.test.ts</files>
  <precondition>Task 1's commit is on `origin main` with a real `conclusion: success` Actions run.</precondition>
  <reversibility rating="reversible">Five new, additive test files — `git revert` removes them cleanly.</reversibility>
  <read_first>
    - themes/lumio/src/lib/utils/textConverter.ts — 9 exported functions (`slugifyyy`, `markdownify`, `humanize`, `titleify`, `plainify`, `toUpperCase`, `toLowerCase`, `toSentenceCase`, `removeWhitespace`); note several already guard `!content` and `console.warn` — test both the guarded-empty path and a real-content path for each
    - themes/lumio/src/lib/utils/sortFunctions.ts — `sortByDate` (sorts by `.data.date`, descending) and `sortByWeight` (ascending by `.weight`, treating `null`/`undefined` as `Infinity`, recursing into `.children` and `.menus`)
    - themes/lumio/src/lib/utils/objectFunctions.ts — `recursiveCloneObject`; the file's own doc comment gives a worked example of the exact independence-from-original behavior to assert
    - themes/lumio/src/lib/utils/splitProtectedText.ts — protects URLs from being split on `/`, splits remaining text on `/` (with optional surrounding whitespace), replaces a year placeholder (default `{{ year }}`, configurable via the `yearPlaceholder` option)
    - themes/lumio/src/lib/utils/trailingSlashChecker.ts — imports `config.site.trailingSlash` from `.astro/config.generated.json`; read that file's current value (`cat themes/lumio/.astro/config.generated.json | node -e "console.log(JSON.parse(require('fs').readFileSync(0)).site.trailingSlash)"`) before writing assertions, since the test must assert behavior matching whatever this config is actually set to, not an assumed value
  </read_first>
  <action>
    Create the five test files, one per module, following `dateFormat.test.ts`'s structure (Task 1) and the existing `getLocalUrlCTM.test.ts`'s import/describe style.

    **textConverter.test.ts** — for each of the 9 exports, one normal-input test and one empty/falsy-input test (most already return `""` on falsy input by design — assert that contract holds). Specific cases worth calling out: `markdownify` with `container=true` vs `container=false` (parse vs parseInline) and with a link href containing `getastrothemes` vs a plain external URL (different `target`/`rel` attributes per the custom renderer); `humanize` with underscores/hyphens/leading-trailing whitespace; `titleify` capitalizing each word; `plainify` stripping HTML tags and decoding entities (`&amp;`, `&lt;`, etc.) from real `marked.parse` output — this exercises `htmlEntityDecoder` indirectly since it's not exported.

    **sortFunctions.test.ts** — `sortByDate`: array of 3+ objects with `.data.date` in unsorted order, assert descending result; edge case: two items with equal dates (stable-ish behavior is acceptable, just assert no crash and correct set membership, not a specific tie-break order). `sortByWeight`: array with defined weights plus at least one `null`/`undefined` weight, assert the undefined-weight item sorts last (`Infinity` behavior); edge case: nested `.children` array, assert it recurses and sorts the children too, and that the original array is not mutated (`.slice()` is used internally — assert by comparing the input array's order is unchanged after the call).

    **objectFunctions.test.ts** — `recursiveCloneObject`: a nested object/array fixture matching the file's own doc-comment example; assert mutating the clone does NOT affect the original (the actual defect class this function exists to prevent); edge cases: primitive input passed directly (returned as-is, not wrapped), and an array containing objects (both array-ness and nested-object-ness must be preserved in the clone).

    **splitProtectedText.test.ts** — a string containing a URL with slashes (e.g. `"Visit https://example.com/path/to/page for info"`) — assert the URL survives intact as one segment, not split on its internal slashes; a string with real `/`-separated segments outside any URL — assert it splits correctly; the default `{{ year }}` placeholder replaced with the actual current year (`new Date().getFullYear()`, computed at test time, not hardcoded, so this test doesn't silently start failing next January); a custom `yearPlaceholder` option (e.g. `{{ YR }}`) replaced correctly, proving the option is honored, not just the default.

    **trailingSlashChecker.test.ts** — read the actual current `config.site.trailingSlash` value first (per `<read_first>` above) and write assertions relative to that value (if `true`: a URL without a trailing slash gains one, one that already has it is unchanged; if `false`: the inverse) — do not assume either value. Edge case: a URL with a `#fragment` — assert the fragment is preserved and reattached after the slash adjustment, not lost or duplicated.

    After all 5 files are written, run the full suite locally (`npm run test:ci`) and confirm all pass, including Task 1's `dateFormat.test.ts`.

    **Mutation check (must_have #4).** Pick ONE newly-added assertion (e.g. `sortByWeight`'s undefined-weight-sorts-last case) and temporarily comment out or invert the one line of production logic it depends on (e.g. `a.weight ?? Infinity` → `a.weight ?? -Infinity` in `sortFunctions.ts`) — confirm the corresponding new test now fails (`npm run test:ci` exits non-zero, with that specific test in the failure list). This proves the test asserts real behavior, not just "does the function run without throwing." Immediately revert the production-code change (`git diff --stat themes/lumio/src/lib/utils/sortFunctions.ts` must show zero diff afterward) and re-run `npm run test:ci` to confirm it's green again before committing anything.

    Record the full per-module accounting for ALL 27 files in `src/lib/utils/` (the 6 covered here, plus every other one) in this plan's SUMMARY.md under a clearly labeled section — this is must_have #3 and is graded on completeness, not brevity. Use three buckets: (a) Covered this phase (the 6), (b) Astro/DOM/filesystem-coupled — cannot be unit-tested without a fundamentally different harness (list each with a one-line reason: `bgOptimizedImage.ts` — imports `astro:assets`; `removeUnusedFonts.ts` — filesystem I/O; `remarkParseContent.ts` — operates on mdast AST via `unist-util-visit`, needs realistic tree fixtures; `AstroFont.ts` — imports `astro/config`'s `fontProviders`; `localizedRouteResolver.ts` — imports `.astro` component files and `astro:content`; `downloadSelfHostedFonts.ts` — filesystem I/O, already `@ts-nocheck`; `FormHandle.ts` — DOM/browser APIs, already typed in v1.0; `i18nUtils.ts` — already has `getLocalUrlCTM.test.ts`), and (c) pure and testable but deliberately deferred — outside this phase's named scope (list each with a one-line note, e.g. "pure, testable, deferred — not named in ROADMAP.md's Phase 5 goal or REQUIREMENTS.md's TECHDEBT-06 examples": `generateTypeScale.ts`, `filteredEnabled.ts`, `handleDraftPage.ts`, `overrideObjects.ts`, `uniqueIdGenerator.ts`, `removeEmptyKeys.ts`, `readingTime.ts`, `buildToc.ts`, `navigationActive.ts`, `getRelatedContent.ts`, `preline.ts`, `absoluteUrl.ts`, `JsonLdGenerator.ts`).
  </action>
  <verify>
    <automated>cd /Users/ryc/projects/lumio2/themes/lumio && npm run test:ci > /tmp/gsd-phase05-task2.log 2>&1; echo "exit=$?"; tail -5 /tmp/gsd-phase05-task2.log</automated>
    <fails_when>`exit=` is anything other than `exit=0`, or the summary line does not show all 6 new suites (dateFormat + 5 from this task) plus the pre-existing one passing</fails_when>

    <automated>cd /Users/ryc/projects/lumio2 && git diff --stat -- themes/lumio/src/lib/utils/ | wc -l | tr -d ' '</automated>
    <fails_when>output is anything other than `0` — any uncommitted diff under `src/lib/utils/` at this point means the mutation-check revert was incomplete</fails_when>

    <automated>gh run list --repo RycBrownrigg/oneMoreGreatIdea --branch main --limit 1 --json databaseId,status,conclusion,headSha</automated>
    <fails_when>the JSON's `conclusion` field is anything other than `"success"`, or `headSha` does not match this task's final pushed commit</fails_when>
  </verify>
  <acceptance_criteria>
    - All 5 new test files exist, each with at least one normal-behavior and one edge-case test
    - `npm run test:ci` passes locally with all 7 suites (6 new + the pre-existing i18n one) green
    - SUMMARY.md contains a complete 27-module accounting (6 covered, ~8 Astro/DOM/filesystem-coupled, ~13 pure-but-deferred) with no module omitted
    - The mutation check is documented in SUMMARY.md: which line was changed, which test caught it, and confirmation the revert left zero diff
    - A real GitHub Actions run for this task's final commit shows `conclusion: success`
  </acceptance_criteria>
  <done>6 utility modules have real, edge-case-inclusive test coverage; every other module in `src/lib/utils/` is accounted for with a recorded reason; a deliberate breakage was proven to fail a real test and was cleanly reverted; CI is green on the real remote.</done>
</task>

</tasks>

<threat_model>
Security enforcement is on for this project at OWASP ASVS level 1, blocking on `high`. This phase adds test files only — no production code path changes, no new dependencies, no new runtime surface. The register is short because the actual security-relevant surface (the CI workflow itself) was already covered in Phase 4's threat model.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file content -> CI execution | New `*.test.ts` files execute as real code inside the CI runner (via Jest); a malicious or badly-written test could theoretically do more than assert, though nothing here does |
| Deliberate mutation probe -> production source | Task 2's mutation check temporarily edits real production logic (`sortFunctions.ts`) on the local working tree before reverting |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01-01 | Tampering | Mutation-check probe on `sortFunctions.ts` | low | mitigate | `<operating_rules>` requires the probe stay uncommitted and local-only, and the task's own verify block checks `git diff --stat` is empty afterward — the probe cannot leak into a real commit even if a step is skipped, because the commit step comes after the revert-and-verify step, not before |
| T-05-01-02 | n/a | New test files | n/a | accept | Test files are plain assertions against already-reviewed production functions; no new dependency, no new I/O, no new external call surface introduced |
| T-05-01-SC | Tampering | npm / pip / cargo package installation | n/a | n/a | Not applicable — no new dependency is installed; `slugify` and `marked` (used by the new `textConverter.test.ts`) are pre-existing devDependencies/dependencies already resolved by the current lockfile |
</threat_model>

<verification>
1. `npm run test:ci` from `themes/lumio/` exits 0 with 7 total suites passing (1 pre-existing + 6 new).
2. Each of the 6 new test files has at least one normal-behavior and one edge-case assertion.
3. SUMMARY.md contains a complete, 27-module accounting with no module omitted.
4. A documented mutation check shows a deliberate breakage failing a real test, then a clean revert (`git diff --stat` empty).
5. Two real GitHub Actions runs (one per task) both show `conclusion: success`.
</verification>

<success_criteria>
- TECHDEBT-06 satisfied: 6 named pure-utility modules have real regression tests covering normal behavior and edge cases.
- ROADMAP Phase 5 success criterion 3 satisfied: every module in `src/lib/utils/` is accounted for, not silently ignored.
- ROADMAP Phase 5 success criterion 4 satisfied: a deliberate breakage was proven to fail a real test, not just "the code still runs."
- ROADMAP Phase 5 success criterion 5 satisfied: the Phase 4 CI workflow picked up all 6 new test files with zero workflow-file edits.
- The 13 pure-but-deferred modules are logged as a real, revisitable backlog item (REQUIREMENTS.md v2 / STATE.md), not lost.
</success_criteria>

## Artifacts this phase produces

- `themes/lumio/src/__tests__/dateFormat.test.ts`
- `themes/lumio/src/__tests__/textConverter.test.ts`
- `themes/lumio/src/__tests__/sortFunctions.test.ts`
- `themes/lumio/src/__tests__/objectFunctions.test.ts`
- `themes/lumio/src/__tests__/splitProtectedText.test.ts`
- `themes/lumio/src/__tests__/trailingSlashChecker.test.ts`

No production source files are modified (aside from the transient, reverted mutation-check probe on `sortFunctions.ts`, which leaves zero diff on disk).

<output>
Create `.planning/phases/05-utility-test-coverage/05-01-SUMMARY.md` when done, including the full 27-module accounting table.
</output>
